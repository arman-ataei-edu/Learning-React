const searchData = (regions, region, language) =>{
    const res = [];
    Object.keys(regions).map((regn) => {
        const countries = [...regions[region]];
        if(region===regn){
            countries.map(country=>{
                if(country.languages)
            })
        }
      })
}